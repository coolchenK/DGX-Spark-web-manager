# DGX Spark Web Manager Frontend

React 19 and Ant Design 5 single-page interface for the DGX Spark management service.

```bash
corepack enable
pnpm install
pnpm dev
pnpm test
pnpm lint
pnpm build
```

During development, Vite proxies `/api` and `/v1` to `http://127.0.0.1:3000`.

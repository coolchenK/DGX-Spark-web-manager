# Product

## Register

product

## Users

DGX Spark 的设备所有者、AI 基础设施管理员和开发者。他们通常在同一局域网内工作，需要在桌面或手机上快速判断设备是否健康、哪些模型正在占用资源，并安全地完成模型下载、部署和故障诊断。

## Product Purpose

DGX Spark Web Manager 将单台 DGX Spark 上分散的 Hugging Face 模型、Docker 推理容器和 OpenAI 兼容端点统一到一个可观察、可控制、可审计的管理平面。成功意味着管理员能从模型发现到 API 调用完成整条工作流，并且所有长任务和 AI 建议都有真实状态、审批和历史记录。

## Brand Personality

克制、精确、可信。界面应像成熟的基础设施工具，强调状态和操作结果，不使用营销式文案或科幻装饰。

## Anti-references

- 不做霓虹渐变、发光边框或玻璃拟态的“AI 控制台”。
- 不做由大量同构指标卡片组成的模板化仪表盘。
- 不隐藏真实错误，也不使用模拟数据冒充设备状态。
- 不为视觉差异重新发明表格、表单、确认框等标准操作方式。

## Design Principles

1. 状态先于装饰：资源、健康、任务和风险应一眼可扫描。
2. 渐进披露：默认展示安全且常用的操作，高风险细节在确认阶段完整展开。
3. 真实反馈：未知、不支持和失败必须明确表达，不使用虚构数据填充界面。
4. 操作可追溯：模型、部署、AI 建议和用户动作都能回到任务与审计记录。
5. 跨设备一致：移动端保留核心管理能力，而不是只提供只读摘要。

## Accessibility & Inclusion

以 WCAG 2.1 AA 为基线。支持键盘导航、可见焦点、语义标签、状态不只依赖颜色、减少动态效果，并确保浅色和深色主题中的文本与状态颜色具有足够对比度。
